import 'package:flutter/material.dart';

class RecordingStudio extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text(
        '1',
      ),
    );
  }
}
